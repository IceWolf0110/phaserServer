spring:
  application:
    name: server

  datasource:
    url: jdbc:mariadb://localhost:3306/chat
    username: root
    password: "01102000"
    driver-class-name: org.mariadb.jdbc.Driver

server:
  port: 8080